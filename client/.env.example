# Copy to .env.local for local overrides, or set as build-time env vars on
# your hosting platform (Vite inlines these at build time, not runtime).

VITE_API_URL=http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000
